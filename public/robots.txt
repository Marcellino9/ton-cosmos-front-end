# Robots.txt — toncosmos.fr
# Fichier maintenu à la main : vite-plugin-sitemap est configuré avec
# generateRobotsTxt: false pour ne pas l'écraser au build (voir vite.config.ts).

User-agent: *
Allow: /

# Bloquer les espaces privés (admin, auth)
Disallow: /administrator/

# Bloquer les ressources non pertinentes pour l'indexation
Disallow: /src/
Disallow: /*.json$

# Sitemaps — le blog est un sous-domaine distinct, on le déclare ici aussi
# pour que Google remonte jusqu'à lui depuis le domaine principal.
Sitemap: https://toncosmos.fr/sitemap.xml
Sitemap: https://blog.toncosmos.fr/sitemap.xml
